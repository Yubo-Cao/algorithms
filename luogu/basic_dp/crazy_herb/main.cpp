#include <cstring>
#include <iostream>

using namespace std;

int main() {
    int n, m;
    cin >> n >> m;

    int v[m], w[m];
    for (int i = 0; i < m; i++) cin >> w[i] >> v[i];

    int dp[m][n];
    memset(dp, 0, sizeof dp);

    for (int i = 1; i < m; i++) {
        for (int j = 0; j < n; j++) {
        }
    }
}